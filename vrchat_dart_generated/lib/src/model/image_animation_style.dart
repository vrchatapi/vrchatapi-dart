//
// AUTO-GENERATED FILE, DO NOT MODIFY!
//

// ignore_for_file: unused_element
import 'package:json_annotation/json_annotation.dart';

/// Animation style for images.
enum ImageAnimationStyle {
  /// Animation style for images.
  @JsonValue(r'aura')
  aura(r'aura'),

  /// Animation style for images.
  @JsonValue(r'bats')
  bats(r'bats'),

  /// Animation style for images.
  @JsonValue(r'bees')
  bees(r'bees'),

  /// Animation style for images.
  @JsonValue(r'bounce')
  bounce(r'bounce'),

  /// Animation style for images.
  @JsonValue(r'cloud')
  cloud(r'cloud'),

  /// Animation style for images.
  @JsonValue(r'confetti')
  confetti(r'confetti'),

  /// Animation style for images.
  @JsonValue(r'crying')
  crying(r'crying'),

  /// Animation style for images.
  @JsonValue(r'dislike')
  dislike(r'dislike'),

  /// Animation style for images.
  @JsonValue(r'fire')
  fire(r'fire'),

  /// Animation style for images.
  @JsonValue(r'idea')
  idea(r'idea'),

  /// Animation style for images.
  @JsonValue(r'lasers')
  lasers(r'lasers'),

  /// Animation style for images.
  @JsonValue(r'like')
  like(r'like'),

  /// Animation style for images.
  @JsonValue(r'magnet')
  magnet(r'magnet'),

  /// Animation style for images.
  @JsonValue(r'mistletoe')
  mistletoe(r'mistletoe'),

  /// Animation style for images.
  @JsonValue(r'money')
  money(r'money'),

  /// Animation style for images.
  @JsonValue(r'noise')
  noise(r'noise'),

  /// Animation style for images.
  @JsonValue(r'orbit')
  orbit(r'orbit'),

  /// Animation style for images.
  @JsonValue(r'pizza')
  pizza(r'pizza'),

  /// Animation style for images.
  @JsonValue(r'rain')
  rain(r'rain'),

  /// Animation style for images.
  @JsonValue(r'rotate')
  rotate(r'rotate'),

  /// Animation style for images.
  @JsonValue(r'shake')
  shake(r'shake'),

  /// Animation style for images.
  @JsonValue(r'snow')
  snow(r'snow'),

  /// Animation style for images.
  @JsonValue(r'snowball')
  snowball(r'snowball'),

  /// Animation style for images.
  @JsonValue(r'spin')
  spin(r'spin'),

  /// Animation style for images.
  @JsonValue(r'splash')
  splash(r'splash'),

  /// Animation style for images.
  @JsonValue(r'stop')
  stop(r'stop'),

  /// Animation style for images.
  @JsonValue(r'zzz')
  zzz(r'zzz');

  const ImageAnimationStyle(this.value);

  final String value;

  @override
  String toString() => value;
}
